/build
/dist
node_modules
.idea
ga.html
.DS_Store
/test-results
/playwright-report
/coverage

# Baseline screenshots (Phase 0 of the TS/React migration, specs/001-modernize-ts-react) are
# regenerated on demand via `npm run build:baseline` — 167MB of PNGs isn't worth committing
# against a ~3.5MB repo. baseline/html and baseline/axe (small, text-diffable) ARE committed.
baseline/png
